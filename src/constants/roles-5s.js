const top = "top";
const jungle = "jungle";
const middle = "middle";
const bottom = "bottom";
const support = "support";

const roles = [top, jungle, middle, bottom, support];

export { top, middle, jungle, bottom, support };
export default roles;
